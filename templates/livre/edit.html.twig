{% extends 'base.html.twig' %}

{% block title %}{{ livre.titre }}{% endblock %}

{% block body %}
    <h1>Modification du livre</h1>

    {{ include('livre/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary mt-4" href="{{ path('app_livre_index') }}">back to list</a>

    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}
